# Mini Data Quality Tool — Project Plan

> **الوثيقة الرسمية المرجعية للفريق أثناء تنفيذ المشروع التدريبي.**  
> الهدف الأساسي هو تطبيق Git وGitHub وClean Code والعمل الجماعي على مشروع Python صغير وواضح، وليس بناء نظام متكامل أو معقد.

## 1. Project Overview

**Mini Data Quality Tool** هو مشروع تدريبي جماعي بسيط بلغة Python يعمل من سطر الأوامر. يقرأ ملف CSV خام، يتحقق من إمكانية استخدامه، ينفذ مجموعة محدودة من عمليات تنظيف البيانات، ثم يحفظ نسخة نظيفة ويعرض تقريرًا مختصرًا بما تم تنفيذه.

الفريق مكوّن من خمسة أعضاء: **لؤي، العنسي، زياد، عمران، حلمي (HelmiMurad)**. يعتمد التنفيذ على تقسيم العمل إلى GitHub Issues وفروع مستقلة وPull Requests مع مراجعة الكود قبل الدمج. مهام حلمي الإضافية مؤجلة خارج التسليم الحالي كما يوضح القسم 21.

## 2. Project Goal

إنشاء أداة صغيرة تحقق الآتي:

1. قراءة ملف CSV خام.
2. التحقق من المدخلات والأخطاء المتوقعة.
3. معالجة القيم المفقودة بطريقة بسيطة وواضحة ومتفق عليها داخل المشروع.
4. إزالة الصفوف المكررة.
5. تنظيف أسماء الأعمدة.
6. حفظ ملف CSV نظيف داخل مجلد `output/`.
7. إنشاء تقرير مختصر يوضح نتائج عمليات التنظيف.
8. تطبيق Workflow عملي باستخدام Git وGitHub وPull Requests والاختبارات وGitHub Actions.

## 3. Scope

يشمل نطاق المشروع فقط:

- برنامج Python يعمل من سطر الأوامر.
- التعامل مع ملفات CSV المحلية.
- التحقق الأساسي من ملف الإدخال.
- تنظيف أسماء الأعمدة.
- التعامل البسيط مع القيم المفقودة.
- حذف الصفوف المكررة.
- إنشاء تقرير مختصر عن عمليات التنظيف.
- حفظ ملف CSV نظيف.
- Unit Tests للوظائف الأساسية.
- GitHub Actions لتشغيل الاختبارات آليًا.
- استخدام GitHub Issues وGitHub Project وPull Requests لتنظيم العمل.

## 4. What Is Out of Scope

لا يتضمن المشروع:

- قواعد بيانات.
- واجهات رسومية GUI.
- Web Frameworks أو APIs.
- تسجيل دخول أو صلاحيات مستخدمين.
- معالجة ملفات ضخمة أو Distributed Processing.
- خوارزميات Data Cleaning متقدمة أو Machine Learning.
- تصميمات معمارية متقدمة أو طبقات غير ضرورية.
- أي ميزات لم تُذكر في متطلبات المشروع.

## 5. Functional Requirements

يجب أن يستطيع المشروع عند اكتماله:

1. استقبال مسار ملف CSV كمدخل للأداة.
2. التحقق من وجود الملف وأنه مناسب للقراءة كملف CSV.
3. قراءة البيانات بنجاح أو إظهار رسالة خطأ واضحة عند فشل القراءة.
4. تنظيف أسماء الأعمدة بصيغة موحّدة وبسيطة.
5. معالجة القيم المفقودة وفق استراتيجية بسيطة موثقة في المشروع.
6. إزالة الصفوف المكررة.
7. حساب معلومات مختصرة عن التنظيف، مثل عدد الصفوف قبل وبعد التنظيف وعدد التكرارات أو القيم المفقودة التي تمت معالجتها عند توفرها.
8. حفظ البيانات النظيفة كملف CSV داخل `output/`.
9. عرض تقرير مختصر بعد انتهاء العملية.
10. إنهاء البرنامج بصورة واضحة عند حدوث خطأ متوقع دون إظهار أخطاء مبهمة للمستخدم.

## 6. Project Structure

```text
mini-data-quality-tool/
├── main.py
├── README.md
├── PROJECT_PLAN.md
├── .gitignore
├── src/
│   ├── __init__.py
│   ├── loader.py
│   ├── adapter.py
│   ├── cleaner.py
│   ├── report.py
│   └── exporter.py
├── tests/
│   ├── test_loader.py
│   ├── test_cleaner.py
│   ├── test_report.py
│   ├── test_exporter.py
│   └── test_main.py
├── data/
│   └── sample.csv
├── output/
│   └── .gitkeep
└── .github/
    └── workflows/
        └── tests.yml
```

**ملاحظات:**

- `main.py`: نقطة تشغيل البرنامج وربط مكونات المشروع.
- `src/loader.py`: قراءة CSV والتحقق من المدخلات.
- `src/adapter.py`: تحويل سجلات التحميل النصية إلى `DataFrame`، والتعرف على الخلايا الفارغة والأعمدة الرقمية قبل التنظيف.
- `src/cleaner.py`: عمليات تنظيف البيانات.
- `src/report.py`: إنشاء ملخص نتائج التنظيف.
- `src/exporter.py`: حفظ الملف النهائي.
- `tests/`: اختبارات الوحدة، مع فصل اختبارات التقرير في `test_report.py` واختبارات التصدير في `test_exporter.py`.
- `data/sample.csv`: ملف تجريبي صغير للاختبار ومسموح برفعه إلى المستودع.
- `output/`: مخصص للملفات الناتجة أثناء التشغيل. لا تُرفع الملفات الناتجة إلى Git، ويُضاف `output/*` إلى `.gitignore` مع استثناء `output/.gitkeep` للإبقاء على المجلد داخل المستودع.

## 7. Team Members and Responsibilities

| العضو | المسؤوليات الأساسية |
|---|---|
| **لؤي** | إعداد Repository والهيكل الأساسي، إعداد GitHub Issues وGitHub Project، تطوير `main.py` وربط المكونات، المساعدة في الدمج النهائي. |
| **العنسي** | تطوير `src/loader.py`، قراءة CSV والتحقق من المدخلات، معالجة أخطاء التحميل المتوقعة، كتابة اختبارات التحميل في `tests/test_loader.py`. |
| **زياد** | تطوير `src/cleaner.py`، معالجة القيم المفقودة، إزالة الصفوف المكررة، تنظيف أسماء الأعمدة، كتابة الاختبارات في `tests/test_cleaner.py`. |
| **عمران** | تطوير `src/report.py` و`src/exporter.py`، إنشاء تقرير التنظيف، حفظ الملف الناتج، كتابة اختبارات التقرير في `tests/test_report.py` واختبارات التصدير في `tests/test_exporter.py`. |
| **حلمي (HelmiMurad)** | تحليل الجودة قبل التنظيف ودليل العرض في #26 و#27؛ مهمتان مؤجلتان وغير منفذتين ضمن التسليم الحالي. |

يمكن لأي عضو مراجعة Pull Request لعضو آخر، لكن لا يدمج صاحب الـ Pull Request عمله دون مراجعة عضو آخر.

## 8. Git Branch Strategy

- الفرع الرئيسي هو `main` ويجب أن يبقى مستقرًا.
- **ممنوع العمل مباشرة على `main`.**
- كل GitHub Issue يجب أن يكون له فرع مستقل.
- يُنشأ الفرع من أحدث نسخة من `main`.
- الأنماط المعتمدة لأسماء الفروع:

```text
feature/name
fix/name
docs/name
chore/name
```

أمثلة مناسبة:

```text
feature/csv-loader
feature/data-cleaning
feature/cleaning-report
feature/csv-exporter
feature/main-integration
chore/github-actions
docs/readme
fix/invalid-csv-handling
```

بعد دمج Pull Request، يُحذف الفرع من GitHub، ويُحذف محليًا عند عدم الحاجة إليه.

## 9. GitHub Workflow

لكل مهمة يتم اتباع التسلسل التالي:

1. إنشاء GitHub Issue واضح للمهمة.
2. إسناد الـ Issue إلى العضو المسؤول.
3. تحديث `main` محليًا قبل بدء العمل.
4. إنشاء فرع مستقل من أحدث `main`.
5. تنفيذ المهمة فقط دون إدخال تغييرات غير مرتبطة بها.
6. تشغيل الاختبارات محليًا.
7. عمل Commits صغيرة وواضحة باستخدام Conventional Commits.
8. Push للفرع إلى GitHub.
9. فتح Pull Request وربطه بالـ Issue.
10. مراجعة الـ Pull Request من عضو آخر.
11. إصلاح الملاحظات وتشغيل الاختبارات مرة أخرى عند الحاجة.
12. دمج Pull Request بعد نجاح المراجعة والاختبارات.
13. حذف الفرع بعد الدمج.
14. يبدأ العضو المهمة التالية من نسخة محدثة من `main`.

## 10. Issues المستخدمة في المشروع

تقسيم مبدئي مقترح للـ Issues:

| Issue | المهمة | المسؤول |
|---|---|---|
| **#1** | إعداد Repository والهيكل الأساسي وملفات المشروع الأولية | لؤي |
| **#3** | إنشاء CSV Loader والتحقق من المدخلات والأخطاء | العنسي |
| **#4** | كتابة Unit Tests للـ Loader | العنسي |
| **#5** | تنفيذ عمليات تنظيف البيانات في `cleaner.py` | زياد |
| **#6** | كتابة Unit Tests لعمليات التنظيف | زياد |
| **#7** | إنشاء تقرير التنظيف في `report.py` | عمران |
| **#8** | تنفيذ تصدير CSV وكتابة اختبارات التقرير والتصدير | عمران |
| **#9** | تطوير `main.py` وربط جميع المكونات | لؤي |
| **#10** | إعداد GitHub Actions لتشغيل الاختبارات | لؤي، مع مراجعة من عضو آخر |
| **#11** | مراجعة المشروع النهائي، تحديث README، وتجربة التدفق الكامل دون إضافة تغييرات وظيفية جديدة | الفريق |
| **#12** | إضافة ملف CSV تجريبي صغير | لؤي |

يمكن تقسيم Issue كبيرة إلى Issue أصغر إذا أصبح نطاقها غير واضح، لكن لا تُضاف ميزات جديدة خارج المتطلبات.

**ملاحظة حول Issue #11:** هذه المهمة للمراجعة النهائية والتجربة والتوثيق فقط. إذا ظهر خطأ أثناء المراجعة، يُفتح له Issue مستقلة وفرع `fix/...` بدل تجميع إصلاحات متعددة داخل Issue #11.

## 11. Pull Request Rules

- كل Pull Request يرتبط بـ Issue محددة.
- لا يُفتح Pull Request يحتوي على تغييرات غير مرتبطة بالمهمة.
- يجب أن يحتوي عنوان الـ Pull Request على وصف واضح للتغيير.
- يجب أن يوضح الوصف: ما الذي تم تنفيذه وكيف تم اختباره.
- لا يتم الدمج قبل مراجعة عضو آخر.
- يجب أن تنجح الاختبارات المحلية وGitHub Actions قبل الدمج.
- عند وجود تعارضات، تُحل قبل الدمج مع التأكد من عدم كسر عمل الأعضاء الآخرين.
- لا يتم دمج كود تجريبي أو Debugging غير مطلوب.
- بعد الدمج يُحذف فرع المهمة.

## 12. Code Review Rules

يراجع العضو المراجع النقاط التالية:

- هل التغيير يحقق الـ Issue فقط؟
- هل الكود واضح وسهل القراءة؟
- هل الأسماء معبّرة؟
- هل الدوال صغيرة وذات مسؤولية واضحة؟
- هل يوجد تكرار غير ضروري؟
- هل أضيف تعقيد غير مطلوب؟
- هل معالجة الأخطاء محددة وواضحة؟
- هل الاختبارات تغطي السلوك الأساسي والتعامل مع الحالات المتوقعة؟
- هل جميع الاختبارات ناجحة؟

المراجعة هدفها تحسين الكود والتعلم الجماعي، وليست إعادة تصميم المشروع أو توسيع نطاقه.

## 13. Commit Message Convention

يستخدم المشروع **Conventional Commits** بالأنواع التالية:

```text
feat:     إضافة وظيفة جديدة ضمن المتطلبات
fix:      إصلاح خطأ
docs:     تعديل التوثيق
test:     إضافة أو تعديل اختبارات
refactor: تحسين بنية الكود دون تغيير السلوك المطلوب
chore:    إعدادات أو مهام صيانة لا تضيف وظيفة للمستخدم
```

> عند كتابة الرسالة فعليًا تُستخدم الكلمات دون المسافات البادئة، مثل `feat: add csv loader`.

أمثلة:

```text
feat: add csv loader
fix: handle missing input file
feat: remove duplicate rows
test: add cleaner tests
refactor: simplify column normalization
docs: update project workflow
chore: add github actions tests
```

يُفضّل أن يكون كل Commit صغيرًا ويمثل تغييرًا منطقيًا واحدًا.

## 14. Testing Strategy

- تستخدم Unit Tests للوظائف الأساسية.
- يجب اختبار حالات النجاح والحالات المتوقعة للفشل.
- `test_loader.py` يختبر تحميل CSV والمدخلات والأخطاء المتوقعة.
- `test_cleaner.py` يختبر تنظيف أسماء الأعمدة والقيم المفقودة وحذف التكرار.
- `test_report.py` يختبر إنشاء التقرير ومحتواه.
- `test_exporter.py` يختبر حفظ ملف CSV الناتج والحالات الأساسية المرتبطة بالتصدير.
- `test_main.py` يختبر ربط المكونات، والملف التجريبي، وتحويل المدخلات، ورسائل الأخطاء ورموز الخروج.
- يجب ألا تعتمد الاختبارات على ملفات كبيرة أو مصادر خارجية.
- يجب أن تكون الاختبارات قابلة للتكرار وسريعة.
- يجب تشغيل جميع الاختبارات قبل فتح أو دمج Pull Request.

## 15. GitHub Actions CI

يحتوي المشروع على Workflow في:

```text
.github/workflows/tests.yml
```

وظيفته الأساسية:

- تشغيل الاختبارات تلقائيًا عند Pull Requests والتغييرات المرتبطة بـ `main`.
- التأكد من أن التغييرات الجديدة لم تكسر الاختبارات الموجودة.
- منع اعتبار Pull Request جاهزًا للدمج إذا فشلت الاختبارات.

يظل الـ CI بسيطًا ومقتصرًا على إعداد بيئة Python وتشغيل الاختبارات المطلوبة للمشروع.

## 16. Clean Code Rules

- الالتزام قدر الإمكان بـ **PEP 8**.
- استخدام أسماء واضحة للمتغيرات والدوال والملفات.
- جعل كل دالة ذات مسؤولية واضحة ومحدودة.
- إبقاء الدوال قصيرة عندما يساعد ذلك على القراءة والفهم.
- تجنب التكرار الواضح، مع عدم تطبيق DRY بصورة مبالغ فيها تزيد التعقيد.
- تطبيق **KISS**: اختيار أبسط حل يحقق المتطلبات.
- تطبيق **YAGNI**: عدم بناء وظائف قد نحتاجها مستقبلًا ما لم تكن مطلوبة الآن.
- عدم إضافة Abstractions أو Classes أو طبقات لا تخدم حاجة واضحة.
- كتابة الكود ليكون سهل القراءة من عضو آخر في الفريق.
- عدم خلط أكثر من مسؤولية غير مرتبطة داخل دالة واحدة.

## 17. Error Handling Rules

- التعامل فقط مع الأخطاء المتوقعة في سياق كل وظيفة.
- استخدام أنواع Exceptions محددة كلما أمكن.
- **ممنوع استخدام `except:` العام دون تحديد نوع الخطأ.**
- عدم إخفاء الخطأ بصمت.
- عرض رسالة مفهومة للمستخدم عند فشل قراءة ملف أو عدم صحة المدخل.
- عدم استخدام Exception Handling للتحكم في التدفق العادي للبرنامج دون حاجة.
- تبقى معالجة الأخطاء بسيطة ومتناسبة مع حجم المشروع.

## 18. Definition of Done

تعتبر الـ Issue مكتملة عندما تتحقق جميع الشروط المناسبة لها:

- تم تنفيذ المطلوب المحدد في الـ Issue فقط.
- الكود يعمل حسب السلوك المتوقع.
- أضيفت أو حُدثت الاختبارات اللازمة.
- نجحت الاختبارات محليًا.
- تم الالتزام بقواعد Clean Code المتفق عليها.
- تم إنشاء Commits واضحة.
- تم رفع الفرع إلى GitHub.
- تم فتح Pull Request وربطه بالـ Issue.
- راجع Pull Request عضو آخر.
- تم التعامل مع ملاحظات المراجعة.
- نجحت GitHub Actions.
- تم دمج Pull Request في `main`.
- تم حذف الفرع بعد الدمج.

## 19. Final Project Workflow

التدفق النهائي المتوقع للمشروع:

```text
CSV Input
   ↓
loader.py
   ↓
Validation / Expected Error Handling
   ↓
adapter.py (String Records → DataFrame)
   ↓
cleaner.py
   ├── Missing Values
   ├── Duplicate Rows
   └── Column Name Cleaning
   ↓
report.py
   ↓
exporter.py
   ↓
Clean CSV in output/
   ↓
Short Cleaning Report
```

يقوم `main.py` بتنسيق هذا التدفق وربط المكونات دون وضع جميع منطق التحميل والتنظيف والتقرير والتصدير داخله.

## 20. Final Deliverables

عند انتهاء المشروع يجب أن يتوفر في `main`:

- الكود النهائي وفق الهيكل المعتمد.
- `PROJECT_PLAN.md` محدثة وتمثل طريقة العمل الفعلية.
- `README.md` يوضح وصف المشروع وطريقة تشغيله بصورة مختصرة.
- `data/sample.csv` لاختبار المشروع.
- اختبارات الوحدة في `tests/` وتكون ناجحة.
- Workflow فعال في `.github/workflows/tests.yml`.
- سجل Git واضح يحتوي على Commits وPull Requests مرتبطة بالعمل.
- GitHub Issues توضح تقسيم المهام وتنفيذها.
- GitHub Project يعرض حالة المهام.
- نسخة مستقرة من المشروع على فرع `main`.

---

**قاعدة الفريق الأساسية:** البساطة، الوضوح، العمل عبر Issues وفروع وPull Requests، والمحافظة على `main` مستقرة طوال المشروع.

## 21. نطاق التسليم وتأجيل التوسعة

بتاريخ 2026-09-22 تقرر تجميد مهمتي حلمي لتسليم النطاق الأساسي دون انتظار توسعة غير منفذة:

| الإيشو | المهمة | الحالة |
|---|---|---|
| [#26](https://github.com/Loai-Alrazi/mini-data-quality-tool/issues/26) | تحليل الجودة قبل التنظيف واختباراته وربطه | مؤجلة، وتبقى مسندة إلى HelmiMurad |
| [#27](https://github.com/Loai-Alrazi/mini-data-quality-tool/issues/27) | دليل العرض العملي للتوسعة | مؤجلة، وتعتمد على #26 عند استئنافها |
| [#25](https://github.com/Loai-Alrazi/mini-data-quality-tool/issues/25) و[PR #28](https://github.com/Loai-Alrazi/mini-data-quality-tool/pull/28) | تجهيز الانضمام وخطة التوسعة | مؤجلان مع التوسعة؛ يبقى PR مسودة خارج الدمج الحالي |

هذه المهام لا تمنع تنفيذ أو دمج إعداد الاختبارات #10 وPR #22، أو المراجعة النهائية #11 وPR #23. تبقى الإيشوز المؤجلة مفتوحة بعلامة `deferred`، وتؤرشف عناصرها من لوحة العمل الحالية دون حذفها أو اعتبارها مكتملة. تبقى مساهمات الأعضاء السابقة محفوظة.

يشمل التسليم الحالي قراءة CSV، وتحويل المدخلات، وتنظيف أسماء الأعمدة والقيم المفقودة والتكرارات، والتصدير وتقرير التنظيف، والاختبارات وGitHub Actions وتوثيق README. لا يتضمن محلل جودة قبل التنظيف أو `docs/DEMO_GUIDE.md`؛ يوفر README خطوات التشغيل والمخرجات اللازمة لعرض النسخة الحالية.

دُمج PR #22 لإعداد الاختبارات وPR #23 للتوثيق بعد مراجعة عضو آخر، ونجحت GitHub Actions على `main`. يُراجع تحديث نطاق التسليم في فرع `docs/delivery-scope` عبر PR مستقل مرتبط بـ #11. تُغلق #11 بعد دمج تحديث النطاق وتجربة التدفق الكامل ونجاح الاختبارات على `main` ونجاح GitHub Actions. لا تتطلب هذه المراجعة اكتمال #25 أو #26 أو #27 أو دمج #28.

عند استئناف التوسعة، تُحدّث خطتها ومسودة #28 من أحدث `main` دون إعادة شروط التعطيل القديمة إلى هذا التسليم، ويُتبع مسار الإيشو والفرع وPR والمراجعة المعتاد.
